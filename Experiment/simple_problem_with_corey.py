# ['Telescopes','Glasses ', 'Eyes', 'Monocles']
# ['Eyes','Glasses ',, 'Monocles', 'Telescopes']

def sort_by_lenght(arr):
    sorted_arr = []
    # for target in arr:
    while arr:
        if len(target) < len(element) and (target not in sorted_arr):
            sorted_arr.append(target)
    
    
    
    
    
    return sorted_arr

print(sort_by_lenght(['Telescopes','Glasses ', 'Eyes', 'Monocles']))